# Deprecated Skills

This folder stores legacy 9-skill layouts preserved during the migration to the single suite skill.

- `deprecated/claude-skills-legacy/`: former `.claude/skills/*` skill directories
- `deprecated/agents-skills-legacy/`: former `.agents/skills/*` skill directories

The active canonical source is `skills-src/critical-debater-suite/`.
Generated targets are `skills/critical-debater-suite/`, `.claude/skills/critical-debater-suite/`, and `openai/skills/critical-debater-suite/`.
